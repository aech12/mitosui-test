import React from "react";
import Button from '../../../output/react/src/components/Button';
import { type ButtonProps } from '../../components/Button.lite';

export default function Task(props: ButtonProps) {
  return (
    <Button>
      Storybook
    </Button>
  );
}
